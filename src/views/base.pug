doctype html
html(lang="kr")
    head 
        title #{pageTitle} | CloneTube
        link(rel="stylesheet" href="https://unpkg.com/mvp.css")
    body
        header 
            nav
                ul 
                    li 
                        a(href="upload") Upload Video
            h1=pageTitle
        main
            block content
    include partials/footer.pug